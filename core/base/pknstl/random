#pragma once

#ifdef USE_STDSTL
#include <random>
#else /*use EASTL*/
#include <random>
#include <EASTL\random.h>
namespace pknstl
{
    using std::random_device; // todo: remove random_device to make kernel compatible
    using std::mersenne_twister_engine;
    using std::uniform_int_distribution;
}
#endif

